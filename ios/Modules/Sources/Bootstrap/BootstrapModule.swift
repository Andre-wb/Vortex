// Bootstrap — Vortex iOS feature module (placeholder for Wave 1 scaffold).
//
// Real interfaces + implementations arrive in subsequent waves, same
// SOLID split the Kotlin client uses:
//   api/   — protocols (abstractions)
//   impl/  — concrete types
//   ui/    — SwiftUI views, where relevant
//
// This placeholder just keeps the target non-empty so SPM doesn't warn.
public enum BootstrapModule {
    public static let version = "0.1.0"
}
