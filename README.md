# Vortex
## 📦 **Установка и запуск**

### 1. Клонировать репозиторий
```bash
git clone https://github.com/Andre-wb/Vortex
cd Vortex
```

### 2. Настроить виртуальное окружение
```bash
python3 -m venv venv
source venv/bin/activate  # для Mac/Linux
# или venv\Scripts\activate  # для Windows
```

### 3. Собрать Rust библиотеку
```bash
cd rust_utils
pip install maturin
maturin develop --release
cd ..
```

### 4. Установить зависимости Python
```bash
cd Vortex
pip install -r requirements.txt
cd ..
```

### 5. Запустить сервер
```bash
cd Vortex
python main.py
```

### 6. Открыть в браузере
```
http://localhost:8000/static/index.html
```

**Для теста открой 2-3 вкладки — они будут общаться между собой!**

## 📁 **Структура проекта**
```
Vortex/
├── vortex_chat/           # Rust библиотека
│   ├── src/
│   │   └── lib.rs
│   └── Cargo.toml
├── vortex-server/         # FastAPI сервер
│   ├── main.py
│   └── requirements.txt
├── static/                # Фронтенд
│   └── index.html
└── venv/                   # Виртуальное окружение
```

## ✅ **Проверка работы**
1. Сервер запущен — видно лог в терминале
2. Браузер открывается — есть интерфейс
3. Сообщения отправляются — приходят другим вкладкам

## 🔧 **Если не работает**
```bash
# Проверь виртуальное окружение
which python  # должен быть путь .../venv/bin/python

# Пересобери Rust библиотеку
cd vortex_chat
maturin develop --release

# Проверь порт 8000 не занят
lsof -i :8000  # для Mac/Linux

