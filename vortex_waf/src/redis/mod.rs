//! Реализации портов WAF поверх общего Redis (`vortex-redis`).
//!
//! Нужны, когда воркеров больше одного: блокировка адреса и его история
//! обращений должны действовать во всех процессах, а не только в том, который
//! их записал. При недоступности Redis обе реализации откатываются в память —
//! так же, как остальной проект.

pub mod block_store;
pub mod record_codec;
pub mod request_history;
pub mod shared;
