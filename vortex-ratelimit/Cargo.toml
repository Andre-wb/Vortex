[package]
name = "vortex-ratelimit"
version = "0.1.0"
edition.workspace = true
description = "Ограничение частоты Vortex — скользящее окно, предметы счёта и пределы точек входа"
license.workspace = true
repository.workspace = true

[lib]
name = "vortex_ratelimit"
path = "src/lib.rs"

[dependencies]
hex = { workspace = true }
parking_lot = { workspace = true }
sha2 = { workspace = true }
