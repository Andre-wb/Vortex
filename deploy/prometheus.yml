# Prometheus configuration for Vortex monitoring
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "vortex"
    static_configs:
      - targets: ["vortex:9000"]
    metrics_path: /metrics
    scrape_interval: 10s
